package pages

import (
	"github.com/bamsy/go-echo-templ/views/layouts"
)

templ Landing() {
    @layout.Base() {
        <div class="relative isolate px-6 pt-14 lg:px-8">
				<div class="text-center">
					<h1 class="text-6xl">
                        Hello from the landing page testing 12345678
                    </h1>
                </div>
        </div>
    }
}